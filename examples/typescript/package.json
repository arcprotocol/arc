{
  "name": "arc-protocol-typescript-examples",
  "version": "1.0.0",
  "description": "TypeScript examples for ARC Protocol",
  "private": true,
  "scripts": {
    "example:server": "ts-node server-basic.ts",
    "example:multi-agent": "ts-node multi-agent-server.ts",
    "example:client": "ts-node client-basic.ts",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "arc",
    "protocol",
    "agent",
    "examples"
  ],
  "author": "ARC Protocol Contributors",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

